apiVersion: core.eda.nokia.com/v1
kind: NodeUser
metadata:
  name: {{ node_user }}
  namespace: {{ namespace }}
spec:
  username: {{ username }}
  password: {{ password }}
  groupBindings:
    - groups:
        - sudo
      nodeSelector:
        - containerlab=managed
  sshPublicKeys: []